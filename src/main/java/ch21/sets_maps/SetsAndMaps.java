package ch21.sets_maps;

public class SetsAndMaps {
    /**
     * Set: store elements with nonduplication rule
     * HashSet
     * LinkedHashSet: same as HashSet but allows to order elements
     * TreeSet: faster for search
     */
}
